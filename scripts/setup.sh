#!/usr/bin/env bash
set -euo pipefail

npm --prefix prompt-structurer install
npm --prefix prompt-transform-worker install

echo "Dependencies installed for prompt-structurer and prompt-transform-worker."
